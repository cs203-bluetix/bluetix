import { render, screen } from "@testing-library/react";
import EventPage from "../../../../src/pages/events/[id]";
describe("Event Page", () => {
  it("should display event page correctly", () => {});
  it("should show a dropdown of all available sessions", () => {});
});
